apiVersion: v1
kind: Service
metadata:
  name: canary-active-svc
  namespace: default
spec:
  ports:
  - port: 80
    targetPort: 8080
    name: http
  selector:
    app: multi-svc-canary
---
apiVersion: v1
kind: Service
metadata:
  name: canary-preview-svc
  namespace: default
spec:
  ports:
  - port: 80
    targetPort: 8080
    name: http
  selector:
    app: multi-svc-canary