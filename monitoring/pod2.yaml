apiVersion: v1
kind: Service
metadata:
  name: canary-active-svc
  namespace: dev
spec:
  ports:
  - port: 80
    targetPort: 8080
    name: http
  selector:
    app: canary-demo
---
apiVersion: v1
kind: Service
metadata:
  name: canary-preview-svc
  namespace: dev
spec:
  ports:
  - port: 80
    targetPort: 8080
    name: http
  selector:
    app: canary-demo